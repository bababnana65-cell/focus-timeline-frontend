# 事件时间轴 App 视觉重设计 · iOS Journal Latest Message Emphasis v28

本目录是 `huashu-design` 交付物，只做视觉设计，不修改 Flutter 业务代码。

## 本版方向

- 单独新建 v28 目录，没有覆盖 v27 或其他版本。
- 以 v27 为基础，只调整“我的关注”专题卡片的文字层级。
- “关注方向”文本退为辅助说明，颜色更轻、字重更低。
- “当前最新节点消息”使用更重字重和浅色高亮，成为卡片里的主阅读重点。
- 最新节点消息的文字起点与关注方向文字左对齐。
- 保留日期与最新节点消息同一行对齐。
- 保留 v27 去掉准备中字样、右箭头和重复“新动态”文字的处理。
- 保留未读红点提示。
- 推荐页左滑关注/取消关注逻辑继承 v25。
- 不使用渐变色。
- 不使用不规则多边形色块。
- 不使用黑色或深色填充块。
- 保留当前截图里的入口、页面结构、分类文案和业务规则。

## 文件

- `event-timeline-latest-message-emphasis.html`：高保真移动端设计板。
- `brand-spec-latest-message-emphasis.md`：专题卡片对齐规范。
- `screenshots/design-board.png`：完整设计板 PNG。
- `screens/*.png`：单页高保真 PNG。
- `figma-handoff.json`：Figma 重建参考 JSON，不是原生 `.fig` 文件。

## 覆盖页面

- 推荐页：当前热门 / 可能关心 / 随机看看 / 历史记录
- 我的关注：普通列表 / 有新动态 / 当前专题 / 空列表
- 时间轴：正常节点列表 / 更多菜单 / 正在准备中 / 初始化失败 / 空时间线
- 新建时间线：输入关键词 / AI 扩写后确认
- 手机号验证弹层
- 关注上限提示
- 刷新失败轻提示
- 组件规范
